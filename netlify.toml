[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Previne que o Netlify bloqueie o build ao detectar a API Key no bundle JS
  SECRETS_SCAN_OMIT_KEYS = "GEMINI_API_KEY"
  NODE_VERSION = "20"
